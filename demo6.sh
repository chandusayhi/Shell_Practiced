#! /bin/bash
echo "cat command is used to display the content of the file to the console"
echo "ls command is used to list the files and directories in the curret directory on the console"
echo "cp command is used to copy the files from source path to the destination path."
echo "mv command is used to rename and move the file from source path to the destination path."
